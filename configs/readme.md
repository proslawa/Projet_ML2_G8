# Configurations YAML pour Hydra

Sections principales :
- `data/` chemins des données
- `eda/` paramètres d'affichage et chargement
- `cleaning et feature engineering/` 
- `modelisation/`
- `mlflow/` tracking des expériences
- `ci/` configuration CI (GitHub Actions)
