name: "XGBoost"
params:
  _target_: xgboost.XGBClassifier
  n_estimators: 100
  learning_rate: 0.1
  max_depth: 6
  random_state: 42
  eval_metric: "logloss"
  use_label_encoder: false

scaler: "robust"

search_space:
  n_estimators: [100, 300]
  learning_rate: [0.01, 0.1]
  max_depth: [4, 6, 8]
