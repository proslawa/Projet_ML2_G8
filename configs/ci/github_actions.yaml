# Configuration CI (GitHub Actions)
python:
  version: "3.10"

commands:
  - "python -m pip install -U pip"
  - "python -m pip install -r requirements.txt"
  - "python -m pytest -q"

artifacts:
  - "reports/"
  - "figures/"
  - "mlruns/"
